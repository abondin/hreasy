import { test } from "@playwright/test";

test.describe("Admin Employee Kids Import", () => {
  test.skip("runs kids import workflow from upload to apply", async () => {});
  test.skip("shows kids-specific preview counters and labels", async () => {});
});
