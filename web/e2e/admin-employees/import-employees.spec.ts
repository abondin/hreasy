import { test } from "@playwright/test";

test.describe("Admin Employees Import", () => {
  test.skip("runs employee import workflow from upload to apply", async () => {});
  test.skip("shows employee-specific preview counters and labels", async () => {});
});
