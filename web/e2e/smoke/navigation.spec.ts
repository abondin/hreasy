import { test } from "@playwright/test";

test.describe("Navigation Smoke", () => {
  test.skip("opens profile, employees, vacations, overtimes, mentorship for authenticated user", async () => {});
  test.skip("opens admin employees tabs for admin user", async () => {});
});
