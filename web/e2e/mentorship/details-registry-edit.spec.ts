import { test } from "@playwright/test";

test.describe("Mentorship Registry Edit", () => {
  test.skip("updates mentor, budget and role in registry card", async () => {});
  test.skip("shows archived dict fallback labels instead of plain ids", async () => {});
});
