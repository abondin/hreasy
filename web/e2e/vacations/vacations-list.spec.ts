import { test } from "@playwright/test";

test.describe("Vacations List", () => {
  test.skip("filters vacations by year, date range, status, project, role and search", async () => {});
  test.skip("switches between list, summary and timeline tabs", async () => {});
  test.skip("shows export action only with export permission", async () => {});
});
