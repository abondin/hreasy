# External read-only API

## Goal and scope

- Design a read-only `/external` API for system-to-system integrations.
- Cover employees, overtime summaries, resource allocation analytics, and projects.
- Authenticate a pair of external system and HR Easy acting user.
- Restrict `/external/**` with one nginx IP allowlist, with localhost-only access by default.
- Implement the approved backend API and nginx allowlist without new persistence or Vue UI.

## Decisions

- Use a dedicated stateless Spring Security chain for `/external/**`.
- Use random opaque Bearer tokens generated by an HR Easy script; external systems never receive a signing key.
- Store only each token's SHA-256 hash and its external-system/acting-user binding.
- Generate one nginx allowlist for `/external/**` from comma-separated `HREASY_EXTERNAL_ALLOWED_IPS`.
- Reuse existing application services and their authorization checks; the external system never bypasses the acting user's permissions or project scope.
- Reuse the existing web response shapes in v1 to keep the implementation small.
- Expose GET operations only. Token issuing, integration administration UI, writes, rate limiting, and response pagination are outside v1.

## Progress

- [x] Inspected the current employee, overtime, allocation, project, Telegram authentication, and WebFlux security flows.
- [x] Added `.docs/external_api_hld.md` with the proposed contract and security model.
- [x] Added nginx proxying and a configurable localhost-only-by-default allowlist for `/external/**`.
- [x] Added hashed opaque-token configuration and authentication for the external-system plus acting-user pair.
- [x] Added an interactive offline token generation script that prints a Docker Compose environment fragment.
- [x] Added the four read-only endpoints over existing application services.
- [x] Added focused opaque-token and overtime-period adapter tests.
- [ ] Review open contract questions with the user.
- [ ] Validate nginx startup; the user chose to run this check locally.

## Validation

- Documentation paths and references were checked against the current backend controllers and services.
- `sh -n web/devops/run.sh` passed.
- `docker compose -f .hreasy-localdev/docker-compose.yml config` passed.
- Focused opaque-token and controller tests passed with the Byte Buddy Java agent required by Mockito on this JDK 25 environment.
- The interactive token generator produced a 64-character token, a matching independently calculated SHA-256, and the requested Docker Compose fields.

## Open questions

- Whether employee responses need `extErpId`; the current basic employee API does not expose it.
- Whether projects need a new external identifier; the current project model has only the HR Easy ID.
- Which concrete external systems and acting users must be configured first.
- Which production CIDRs nginx must allow and whether another proxy is deployed in front of nginx.
