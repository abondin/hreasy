# Active Tasks

This directory keeps concise, source-controlled context for work that spans multiple iterations.

Each active task has one Markdown file. The file is updated as requirements, decisions, implementation, and verification evolve. After the user confirms completion, durable documentation and `changelogs/CHANGELOG.md` are updated and the task file is removed.
